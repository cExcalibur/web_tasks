<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <title> </title>

  </head>
  <body>
    <%= form_tag("/front/frontend/submitfeedback", method: "post") do %>
      意见反馈: <%= text_area_tag(:feedback) %>
      </br>
      昵称: <%= text_field_tag(:nickname) %>
      </br>
      <%= submit_tag("提交") %>
    <% end %>
    <%= button_to '返回', {:controller => 'front/frontend', :action => 'index'}, :method => 'get' %>
  </body>
</html>
